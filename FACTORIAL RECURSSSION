#include<stdio.h>
int factorial(int n);
int main()
{
	int n;
	printf("enter the number");
	scanf("%d", &n );
	printf("\n the factorial of %d is %d\n ", n, factorial(n));
	return 0;
}
int factorial (int n)
{

int i,fact=1;
	for(i=0;i<=n;i++)
	{
		fact=fact*i;
}
return fact;
}
